# Password Generator PWA

Plain HTML/CSS/JavaScript demo for learning PWA -> Android packaging.

## Run
Serve this folder over HTTP, for example:
`python -m http.server 8080`
Then open `http://localhost:8080`.

For PWABuilder, deploy it to an HTTPS URL first and submit that URL.
