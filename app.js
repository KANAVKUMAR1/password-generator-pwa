const $=id=>document.getElementById(id);
const chars={uppercase:"ABCDEFGHIJKLMNOPQRSTUVWXYZ",lowercase:"abcdefghijklmnopqrstuvwxyz",numbers:"0123456789",symbols:"!@#$%^&*()_+-=[]{}|;:,.<>?"};
function rnd(max){const a=new Uint32Array(1);crypto.getRandomValues(a);const lim=Math.floor(0x100000000/max)*max;return a[0]<lim?a[0]%max:rnd(max)}
function generate(){const len=+$('length').value;const keys=Object.keys(chars).filter(k=>$(k).checked);if(!keys.length){$('message').textContent='Select at least one character type.';return}const pool=keys.map(k=>chars[k]).join('');let out=[];keys.forEach(k=>{const s=chars[k];out.push(s[rnd(s.length)])});while(out.length<len)out.push(pool[rnd(pool.length)]);for(let i=out.length-1;i>0;i--){const j=rnd(i+1);[out[i],out[j]]=[out[j],out[i]]}$('password').value=out.join('');const score=Math.min(100,Math.max(20,(len-8)*4+keys.length*20));$('strengthText').textContent=score>=70?'Strong':score>=45?'Medium':'Weak';$('strengthBar').style.width=score+'%';$('message').textContent=''}
$('length').oninput=()=>{$('lengthValue').textContent=$('length').value;generate()};['uppercase','lowercase','numbers','symbols'].forEach(id=>$(id).onchange=generate);$('generateBtn').onclick=generate;$('copyBtn').onclick=async()=>{try{await navigator.clipboard.writeText($('password').value);$('message').textContent='Password copied!'}catch{$('password').select();document.execCommand('copy');$('message').textContent='Password copied!'}};if('serviceWorker'in navigator)addEventListener('load',()=>navigator.serviceWorker.register('sw.js'));generate();
